"""Define application-specific exceptions."""


class NewFeatureError(RuntimeError):
    """Report an expected error that should be shown to the CLI user."""

    """User-facing error raised by the feature-worktree CLI."""
